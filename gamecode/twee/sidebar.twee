:: PassageHeader
<<set $passage to passage()>><<set $tags to tags($passage)>>


:: StoryCaption

<<if $passage == 'Start' || $passage == 'Start1' >>
一个用于制作游戏的范本，稍微能玩的版本。<br>
 version 0.1.3<br>
 author: 狐千月<br>
<<else>>

/* 这里显示目前操作角色的信息 */

<</if>>

<div id='random_tips'>
</div>
<div id='update_note'>
</div>

:: PassageFooter


:: PassageReady
<<run
    _passageready = true;
    if($lastsession !== $passage)
      $lastsession = $passage;

    //复原角色的class
    if(G.onLoad || G.onReload){
        F.LoadCharacters();
        G.onLoad = false;
        G.onReload = false;
    };
    $player = C[pc];
    $target = C[tc];
    
    if($mode !== 'train')
      $flag.mode = 0;

>>

:: PassageDone


:: StoryHeader
<<if !groupmatch($passage, 'Start', 'Start1')>>
<div id='showtime' class='headbar-container'>
    <<showtime>>
    <<showmoney>>
</div>
<div id='gametime'>
    <<showgametime>>
</div>
<<repeat 1s>>
    <<replace '#gametime'>>
        <<showgametime>>
    <</replace>>
<</repeat>>
<</if>>

:: StoryFooter

